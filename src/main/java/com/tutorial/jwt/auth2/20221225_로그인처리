A. 유저 네임, 패스워드 로그인 정상처리.

서버쪽에 세션ID생성
클라이언트의 쿠키에 세션ID를 응답

요청할때마다 쿠키값 세션ID를 항상 들고 서버쪽으로 요청하기 때문에
서버는 세션ID가 유효한지 판단해서 인증이 필요한 페이지로 접근을 하게 되면 된다.


B. 유저네임, 패스워드 로그인 정상처리.
JWT 토큰 생성
클라이언트 쪽으로 JWT 토큰을 응답

요청할때마다 JWT토큰을 가지고 요청
서버는 JWT토큰이 유효한지를 판단(필터를 만들어줘야 함)
